name: NPM Publish (Production — Legacy)

# ─────────────────────────────────────────────────────────────────────────────
# ⚠️  DEPRECATED — this file is kept for reference only.
#
#    The production publish pipeline has been replaced by `release.yml`.
#    `release.yml` is triggered by pushing a git tag (e.g. `v1.1.4`) and
#    handles version validation, NPM publish with provenance, and GitHub
#    Release creation in separate, well-scoped jobs.
#
#    DO NOT enable this workflow. It has no triggers.
# ─────────────────────────────────────────────────────────────────────────────
on:
  workflow_dispatch:
    inputs:
      confirm:
        description: 'Type "i-know-what-i-am-doing" to run the legacy path'
        required: true

jobs:
  legacy-publish:
    name: ⚠️ Legacy publish (disabled)
    runs-on: ubuntu-latest
    if: github.event.inputs.confirm == 'i-know-what-i-am-doing'
    steps:
      - run: |
          echo "::error::This workflow is deprecated. Use release.yml instead."
          exit 1
